-- Outline using tags (works standalone). Best structured.
-- Still: SymbolsOutline is based on LSP.

local M = {
    'majutsushi/tagbar',
    config = function()
        vim.g.tagbar_width = 60
    end
}

return M
